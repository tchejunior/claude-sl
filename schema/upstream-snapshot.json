{
  "fetched_at": "2026-09-10T12:10:43.201549+00:00",
  "source_url": "https://code.claude.com/docs/en/statusline",
  "fields": [
    {
      "path": "model.id",
      "description": "Current model identifier and display name"
    },
    {
      "path": "model.display_name",
      "description": "Current model identifier and display name"
    },
    {
      "path": "cwd",
      "description": "Current working directory. Both fields contain the same value; workspace.current_dir is preferred for consistency with workspace.project_dir."
    },
    {
      "path": "workspace.current_dir",
      "description": "Current working directory. Both fields contain the same value; workspace.current_dir is preferred for consistency with workspace.project_dir."
    },
    {
      "path": "workspace.project_dir",
      "description": "Directory where Claude Code was launched, which may differ from cwd if the working directory changes during a session"
    },
    {
      "path": "workspace.added_dirs",
      "description": "Additional directories added via /add-dir or --add-dir. Empty array if none have been added"
    },
    {
      "path": "workspace.git_worktree",
      "description": "Git worktree name when the current directory is inside a linked worktree created with git worktree add. Absent in the main working tree. Populated for any git worktree, unlike worktree.*, which is present only while the session is in a worktree session"
    },
    {
      "path": "workspace.repo.host",
      "description": "Repository identity parsed from the origin remote, for example, \"github.com\", \"anthropics\", \"claude-code\". Absent outside a git repository or when no origin remote is configured. For a gitlab.com project nested in subgroups, owner is the full namespace path with slashes, such as \"group/subgroup\". Before v2.1.260, workspace.repo was absent for these projects"
    },
    {
      "path": "workspace.repo.owner",
      "description": "Repository identity parsed from the origin remote, for example, \"github.com\", \"anthropics\", \"claude-code\". Absent outside a git repository or when no origin remote is configured. For a gitlab.com project nested in subgroups, owner is the full namespace path with slashes, such as \"group/subgroup\". Before v2.1.260, workspace.repo was absent for these projects"
    },
    {
      "path": "workspace.repo.name",
      "description": "Repository identity parsed from the origin remote, for example, \"github.com\", \"anthropics\", \"claude-code\". Absent outside a git repository or when no origin remote is configured. For a gitlab.com project nested in subgroups, owner is the full namespace path with slashes, such as \"group/subgroup\". Before v2.1.260, workspace.repo was absent for these projects"
    },
    {
      "path": "cost.total_cost_usd",
      "description": "Estimated session cost in USD, computed client-side at list price unless a modelPricing table is in effect. May differ from your actual bill. Resets to $0 when /clear starts a new session. Before v2.1.211, the total carried over after /clear"
    },
    {
      "path": "cost.total_duration_ms",
      "description": "Total wall-clock time since the session started, in milliseconds"
    },
    {
      "path": "cost.total_api_duration_ms",
      "description": "Total time spent waiting for API responses in milliseconds"
    },
    {
      "path": "cost.total_lines_added",
      "description": "Lines of code changed"
    },
    {
      "path": "cost.total_lines_removed",
      "description": "Lines of code changed"
    },
    {
      "path": "context_window.total_input_tokens",
      "description": "Token counts currently in the context window, from the most recent API response. Input includes cache reads and writes"
    },
    {
      "path": "context_window.total_output_tokens",
      "description": "Token counts currently in the context window, from the most recent API response. Input includes cache reads and writes"
    },
    {
      "path": "context_window.context_window_size",
      "description": "Maximum context window size in tokens. 200000 by default, or 1000000 for models with extended context."
    },
    {
      "path": "context_window.used_percentage",
      "description": "Pre-calculated percentage of context window used"
    },
    {
      "path": "context_window.remaining_percentage",
      "description": "Pre-calculated percentage of context window remaining"
    },
    {
      "path": "context_window.current_usage",
      "description": "Token counts from the last API call, described in context window fields"
    },
    {
      "path": "exceeds_200k_tokens",
      "description": "Whether the total token count (input, cache, and output tokens combined) from the most recent API response exceeds 200k. This is a fixed threshold regardless of actual context window size."
    },
    {
      "path": "effort.level",
      "description": "Current reasoning effort (low, medium, high, xhigh, or max). Reflects the live session value, including mid-session /effort changes. Ultracode is not a distinct level and reports as xhigh. Absent when the current model does not support the effort parameter"
    },
    {
      "path": "thinking.enabled",
      "description": "Whether extended thinking is enabled for the session"
    },
    {
      "path": "rate_limits.five_hour.used_percentage",
      "description": "Percentage of the 5-hour or 7-day rate limit consumed, from 0 to 100"
    },
    {
      "path": "rate_limits.seven_day.used_percentage",
      "description": "Percentage of the 5-hour or 7-day rate limit consumed, from 0 to 100"
    },
    {
      "path": "rate_limits.five_hour.resets_at",
      "description": "Unix epoch seconds when the 5-hour or 7-day rate limit window resets"
    },
    {
      "path": "rate_limits.seven_day.resets_at",
      "description": "Unix epoch seconds when the 5-hour or 7-day rate limit window resets"
    },
    {
      "path": "rate_limits.spend_limit.used_percentage",
      "description": "Behind a Claude apps gateway, the percentage used of the spend limit that applies to you, and the Unix epoch seconds when its period resets. The percentage runs from 0 to 100, or above 100 once you exceed the limit. Requires Claude Code v2.1.251 or later"
    },
    {
      "path": "rate_limits.spend_limit.resets_at",
      "description": "Behind a Claude apps gateway, the percentage used of the spend limit that applies to you, and the Unix epoch seconds when its period resets. The percentage runs from 0 to 100, or above 100 once you exceed the limit. Requires Claude Code v2.1.251 or later"
    },
    {
      "path": "session_id",
      "description": "Unique session identifier"
    },
    {
      "path": "session_name",
      "description": "Session name. Uses the custom name set with the --name flag or /rename when one exists, otherwise the AI-generated session title. The default display name, such as my-app-3f, doesn\u2019t populate this field. Absent when the session has neither a custom name nor an AI-generated title"
    },
    {
      "path": "transcript_path",
      "description": "Path to conversation transcript file"
    },
    {
      "path": "version",
      "description": "Claude Code version"
    },
    {
      "path": "output_style.name",
      "description": "Name of the current output style"
    },
    {
      "path": "vim.mode",
      "description": "Current vim mode (NORMAL, INSERT, VISUAL, or VISUAL LINE) when vim mode is enabled"
    },
    {
      "path": "agent.name",
      "description": "Agent name when running with the --agent flag or agent settings configured"
    },
    {
      "path": "pr.number",
      "description": "Open pull request for the current branch. Mirrors the PR badge in the footer. In a repository with a GitLab remote, Claude Code fills these fields from the branch\u2019s open merge request instead, so pr.number is the merge request number. Merge request data requires Claude Code v2.1.234 or later. Absent when not in a git repository, until a pull request or merge request is found, or once it merges or closes"
    },
    {
      "path": "pr.url",
      "description": "Open pull request for the current branch. Mirrors the PR badge in the footer. In a repository with a GitLab remote, Claude Code fills these fields from the branch\u2019s open merge request instead, so pr.number is the merge request number. Merge request data requires Claude Code v2.1.234 or later. Absent when not in a git repository, until a pull request or merge request is found, or once it merges or closes"
    },
    {
      "path": "pr.review_state",
      "description": "Review status of the open PR: approved, pending, changes_requested, or draft. May be independently absent even when pr is present"
    },
    {
      "path": "pr.kind",
      "description": "mr when pr describes a GitLab merge request. Absent for GitHub pull requests, so scripts written before this field keep working. For a merge request, Claude Code sets review_state to approved when GitLab reports it mergeable, pending for any other open state, and draft for a draft. Requires Claude Code v2.1.234 or later"
    },
    {
      "path": "worktree.name",
      "description": "Name of the active worktree. Present only while the session is in a worktree session"
    },
    {
      "path": "worktree.path",
      "description": "Absolute path to the worktree directory"
    },
    {
      "path": "worktree.branch",
      "description": "Git branch name for the worktree (for example, \"worktree-my-feature\"). Absent for hook-based worktrees"
    },
    {
      "path": "worktree.original_cwd",
      "description": "The directory Claude was in before entering the worktree"
    },
    {
      "path": "worktree.original_branch",
      "description": "Git branch checked out before entering the worktree. Absent for hook-based worktrees"
    }
  ],
  "enums": {
    "effort.level": [
      "low",
      "medium",
      "high",
      "xhigh",
      "max"
    ],
    "vim.mode": [
      "NORMAL",
      "INSERT",
      "VISUAL",
      "VISUAL LINE"
    ]
  }
}
